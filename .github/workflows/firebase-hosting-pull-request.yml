# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 14.x
        uses: actions/setup-node@main
        with:
          node-version: 14.x
      - name: Get yarn cache directory path
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"

      - uses: actions/cache@v2
        id: yarn-cache # use this to check for `cache-hit` (`steps.yarn-cache.outputs.cache-hit != 'true'`)
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-
      - run: yarn install --frozen-lockfile
      - run: yarn build
        env:
          NODE_ENV: production
          VITE_FIREBASE_API_KEY: AIzaSyCb7qguIyONBldSGRXiZolhNyHyVezTuPI
          VITE_FIREBASE_AUTH_DOMAIN: bcviz-6b514.firebaseapp.com
          VITE_FIREBASE_PROJECT_ID: bcviz-6b514
          VITE_FIREBASE_STORAGE_BUCKET: bcviz-6b514.appspot.com
          VITE_FIREBASE_MESSAGING_SENDER_ID: 960890617921
          VITE_FIREBASE_APP_ID: 1:960890617921:web:32ce3f89d7b3d7841986c0
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_BCVIZ_6B514 }}'
          projectId: bcviz-6b514
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
